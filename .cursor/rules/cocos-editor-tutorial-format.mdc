---
description: Cocos 编辑器挂接/教程输出格式 — 必须标明面板、节点、拖放目标类型（Label/Sprite/节点/组件）
alwaysApply: true
---

# Cocos 编辑器教程输出规范（强制）

凡向用户说明 **Cocos Creator 编辑器操作**（预制体、场景挂接、资源设置、Inspector 绑定等），**必须**按本规范书写，禁止模糊表述如「拖进去」「在编辑器里设置」而不指明位置与类型。

## 1. 必须标明的面板与文件

每条操作步骤须写明 **在哪个 Cocos 面板** 操作；若涉及工程文件，须写明 **完整路径**。

| 面板（中文名） | 英文/别名 | 典型用途 |
|----------------|-----------|----------|
| **层级管理器** | Hierarchy | 选节点、建子节点、看父子关系 |
| **资源管理器** | Assets | 选 `.scene` / `.prefab` / 图片 / 脚本 |
| **属性检查器** | Inspector / Properties | 改组件属性、拖引用、设数值 |
| **场景编辑器** | Scene | 摆放位置、对齐坐标（仅当需要时写） |
| **控制台** | Console | 报错排查（仅当需要时写） |

**打开场景/预制体示例**：
> 在 **资源管理器** 中双击 `assets/prefabs/ui/ItemCardWidget.prefab`，在 **层级管理器** 中选中根节点 `ItemCardWidget`。

## 2. 创建内容：四项必填

| 字段 | 说明 | 示例 |
|------|------|------|
| **父节点** | 在层级管理器中挂在谁下面 | 父节点：`CdOverlay` |
| **名称** | 节点/资源确切命名 | 名称：`CdTimeLabel` |
| **类型** | 创建菜单的完整类型 | 类型：**UI → Label** |
| **组件**（若需） | 在属性检查器中添加的脚本或内置组件 | 添加组件：`TagIconStrip` |

## 3. 赋值与配置：必须写「属性检查器」

> 在 **属性检查器** 中，选中节点 `XXX`，在组件 `YYY` 的 `属性名` 处配置为 `值`。

## 4. 拖放：必须写清方向 + **拖入类型**（核心）

### 4.1 固定句式（方向）

```
在【属性检查器】选中【目标节点】→ 在【组件名】的【属性名】
→ 从【层级管理器】拖入【层级路径】上的【拖入类型】
```

或数组槽位：

```
→ 拖到【组件名】→【数组属性名】→ Element N
```

### 4.2 **拖入类型**标记（强制，不可省略）

凡拖放绑定，**必须**在说明末尾或表格「拖入类型」列标明下列之一（加粗）：

| 标记 | 含义 | 拖什么 | 常见误操作 |
|------|------|--------|------------|
| **节点** | Node 引用 | 拖 **层级管理器里的节点名**（不要求特定组件） | 误拖成 Sprite |
| **Label 组件** | `@property(Label)` | 展开节点 → 拖 **Label** 那一行，或拖节点后确认类型为 Label | 只拖节点名但不确认组件 |
| **Sprite 组件** | `@property(Sprite)` | 拖 **Sprite** 组件 | 拖成节点或 UITransform |
| **Button 组件** | `@property(Button)` | 拖 **Button** 组件 | 拖成节点 |
| **{脚本名} 组件** | 自定义 `@ccclass` | 拖该脚本组件，如 **ItemStarBadge 组件**、**TagIconStrip 组件** | 拖成节点 |
| **资源** | SpriteFrame / Prefab 等 | 从 **资源管理器** 拖 `.png` / `.prefab` | 从层级拖节点 |
| **留空** | 可选字段 | 写「留空」并简述运行时行为 | — |

**禁止**：
- 「拖到 Icon 上」而不写 **Sprite 组件** 还是 **节点**
- 「绑 Label」而不写是 **Label 组件** 还是 **名为 Label 的节点**
- 表格只有「拖入目标」列、没有「拖入类型」列

### 4.3 绑定清单：推荐用三列表格

多条属性绑定时，**必须**用表格，且含 **拖入类型** 列：

| 属性名（属性检查器） | 层级路径 | 拖入类型 |
|----------------------|----------|----------|
| **Name Label** | `ItemNameLabel` | **Label 组件** |
| **Cd Overlay Node** | `CdOverlay` | **节点** |
| **Cd Overlay Bar** | `CdOverlay` → `CdOverlayBar` | **Sprite 组件** |
| **Tag Icons → Element 0** | `TagContainer` → `TagIcon_0` | **Sprite 组件** |

表格前加一句操作前提：

> 在 **层级管理器** 选中根节点 `ItemCardWidget` → **属性检查器** → **Item Card Widget** 组件，按下表拖入（带「组件」的项请拖 **该组件**，不要只拖节点名）。

### 4.4 子节点脚本：单独成节

若预制体上还有 **子节点脚本**（如 `TagIconStrip`、`ItemModBadge`），另起一节「子节点上的脚本」，同样用三列表格，避免与主脚本混淆。

### 4.5 层级树速查（可选但推荐）

绑定清单后附 ASCII 树，节点后标注组件类型：

```
ItemCardWidget          ← ItemCardWidget + Button
├── Icon                ← Sprite
├── ItemNameLabel       ← Label
└── CdOverlay           ← 节点
    └── CdTimeLabel     ← Label
```

## 5. 推荐步骤模板

```markdown
### 步骤 N：{标题}

- **面板**：层级管理器 / 属性检查器
- **文件**：`assets/...`
- **操作**：
  1. 在 **层级管理器** 选中 `{节点}`。
  2. 在 **属性检查器** → `{组件}` → `{属性}`。
  3. 从 **层级管理器** 的 `{路径}` 拖入，类型：**{Label 组件 / Sprite 组件 / 节点 / …}**。
```

## 6. 节点树路径

描述结构时用完整路径：`Canvas/ItemSlots/ItemCardWidget_0/CdOverlay/CdTimeLabel`。

## 7. 与代码/文档的关系

- 编辑器操作不得仅用代码 `@property` 名代替面板说明；表格「属性名」应与 **属性检查器显示名** 一致（Pascal/camel 均可，但要能对上）。
- 更新 `Docs/` 下 Cocos 交接文档时，同步遵守本规范。
